# AI Work Log — Legacy vs Svelte Reconciliation

Сверка src-legacy/* (vanilla JS/Tauri) vs src/* (SvelteKit) завершена —
все модули построчно/детально сопоставлены, legacy-код удалён из репозитория
(src-legacy/), приложение полностью живёт на SvelteKit.

## Итог сверки — найдено и исправлено 9 расхождений
1. ChaptersScreen.svelte: стрелки ↑/↓ не обновляли список вопросов
   редактора (не вызывали refreshEditorQuestions()).
2. ResultScreen.svelte: активная точка в review-grid не скроллилась в
   видимую область при навигации стрелками по разбору ответов.
3. QuestionFormModal.svelte: submit() молча делал return при невалидной
   форме вместо toast-сообщения.
4. EditorTooltip.svelte: не мерил реальные размеры элемента (offsetWidth/
   offsetHeight) и не клампил по краям окна — длинная подсказка у края
   могла вылезать за экран.
5. Общий баг во всех модалках-формах: не автофокусился первый input при
   открытии (легаси openModal() это делал) — исправлено одним фиксом в
   Modal.svelte.
6. MenuScreen.svelte: держал собственную устаревшую копию menuConfirm(),
   старт экзамена из меню вёл на пустой экран (beginQuiz не вызывался).
7. ConfirmDialog.svelte: не проставлял класс "danger" на .modal-box и
   id="modal-title" — у опасных подтверждений пропадала красная рамка.
8. ToastStack.svelte: красил тосты через data-kind вместо ожидаемых CSS
   классов .toast-error/.toast-success/.toast-info — цветовая индикация
   не работала совсем; плюс не было fade-out анимации перед удалением.
9. StatusBar.svelte: отсутствовала ветка для connectionStatus==="degraded"
   (показывало литералом "Сервер: degraded"), и не было title-тултипа
   с деталями статуса.

Все остальные модули (api.js, cache.js, config.js, questions.js, quiz.js,
auth.js, device.js, admin.js, friends.js, все .svelte-компоненты) сверены
и являются точным или документированно-улучшенным портом легаси —
дополнительных расхождений не найдено.

## Дополнительно найдено после "закрытия" всех этапов
10. HintBar.svelte — два расхождения:
    a) подсказки рендерились плоским текстом ("↑/↓ выбрать · Enter принять")
       вместо чипов <kbd> (легаси: каждая группа — .hint-group с <kbd> на
       каждую клавишу) — CSS для чипов (.hint-group, kbd, .hint-label)
       существовал, но никогда не применялся.
    b) панель не пряталась (класс "hidden") ни при пустых подсказках, ни на
       экране меню — легаси всегда скрывает hint-bar в обоих случаях
       (на меню подсказки не нужны, там цифры 1-9 видны на самих пунктах),
       и раскладка .stage зависит от #app-shell:has(.hint-bar.hidden).
    Оба фикса внесены.

## Найдено после добавления SvelteKit — пропущенный шаг сборки фронтенда
11. flatpak_data/roadwits-client.flatpak.yaml — build-commands модуля
    "roadwits-client" делали `npm ci` и сразу `cargo build --release`,
    без `npm run build` между ними. В легаси vanilla-JS фронтенд не
    требовал сборки (статика лежала в репозитории как есть), поэтому
    `cargo build` работал сразу — tauri_build::build() (build.rs)
    встраивает содержимое frontendDist ("../build" в tauri.conf.json)
    в бинарник на этапе компиляции. После миграции на SvelteKit каталог
    "../build" создаётся только `vite build` (npm run build), а сам
    он не в репозитории (не трекается git, .gitignore его тоже не
    перечислял). Без этого шага Flatpak-сборка либо падает (нет
    ../build), либо (при наличии старого каталога) зашивает устаревший
    фронтенд. Добавлен шаг `npm run build` перед cargo build; заодно
    добавлен `build/` в .gitignore (не был перечислён после миграции).
    Обычный релиз через tauri-action (job "release" в release.yml)
    этой проблеме не подвержен — tauri-action сам вызывает
    beforeBuildCommand ("npm run build") из tauri.conf.json, это уже
    сверено ранее.


## Найдено при первом dev-запуске (npm/Rust уже стояли, приложение работало) — 6 svelte-check warnings
12. a11y_no_noninteractive_element_to_interactive_role (4 места: MenuScreen,
    ChaptersScreen — список глав, RandomCountScreen, QuestionScreen —
    список вариантов ответа) — `<li role="button" tabindex="0" onclick
    onkeydown>` напрямую на `<li>` внутри `<ul>` ломает семантику списка
    для скринридеров (Svelte 5 предупреждает об этом явно, в отличие от
    Svelte 4). Исправлено переносом role/tabindex/обработчиков и класса
    стилизации на вложенный `<div>` внутри пустого `<li>` — визуально и
    по CSS ничего не меняется (все селекторы `.menu-item .m-index` и
    т.п. — descendant, не child-комбинаторы, `.menu-list`/`.chapter-list`
    flex-layout остаётся на уровне `<ul>` → `<li>`, не затронут).
    Проверено: `svelte-check` — warnings 10 → 0; `npm run build` проходит
    без ошибок.
13. state_referenced_locally (QuestionFormModal.svelte, ChapterFormModal.svelte,
    6 упоминаний) — локальные `$state` поля формы (text/hint/answers/
    title/description) инициализировались чтением реактивного пропа
    (question/chapter) без явного обозначения "только начальное
    значение" — Svelte 5 предупреждает, что это похоже на забытый
    `$derived`. По факту это осознанный паттерн (снимок на момент
    открытия, дальше редактируется локально), и баг был не в текущем
    поведении (родитель — ChaptersScreen.svelte — всегда закрывает
    модалку перед открытием другой цели), а в хрупкости: любое будущее
    изменение, при котором editingQuestion/editingChapter поменяется
    с одного объекта на другой без прохождения через null, тихо оставило
    бы в форме старые данные. Исправлено на два уровня: (1) в самих
    компонентах — обёрнуто в `untrack(() => ...)` из "svelte", что явно
    документирует намерение и убирает warning; (2) в ChaptersScreen.svelte
    — вызовы `<QuestionFormModal>`/`<ChapterFormModal>` обёрнуты в
    `{#key editingQuestion}`/`{#key editingChapter}`, что гарантирует
    полный ремонт компонента при смене цели редактирования независимо
    от будущих изменений логики — защита от регресса.

## Найдено пользователем — баг с кнопкой "Войти" после logout
14. auth.js: state.loginSubmitting сбрасывался в false только в catch
    (ветка ошибки submitLogin), но не в ветке успеха. После успешного
    входа флаг так и оставался true (незаметно, т.к. экран логина уже
    скрыт), а после logout() (который его тоже не трогал) LoginView
    показывался снова с застрявшим loginSubmitting === true — кнопка
    "Войти" превращалась в задизейбленную "Входим..." навсегда, до
    перезапуска приложения. Исправлено: submitLogin сбрасывает флаг
    и на успешном пути тоже; logout() дополнительно сбрасывает его
    сам как defensive reset (на случай будущих путей выхода, которые
    могли бы забыть это сделать) — по аналогии с уже имеющимся сбросом
    timerHandle в этой же функции.

Полноценных нерешённых задач по сверке не осталось. Если появятся идеи
по улучшениям (баги, новые фичи, стили, анимации) — можно реализовывать
их напрямую, отмечая здесь при необходимости.
- [x] package.json не обновляется — исправлено: добавлена переменная PACKAGE_JSON и sed-замена версии (первое вхождение "version" в файле — это и есть поле version, "name" стоит раньше и sed-паттерном "version": ... не задевается). Проверено на тестовых файлах.
- [x] git fetch origin без --tags — исправлено: `git fetch origin --tags`.
- [x] Скрипт не переходит в корень репозитория — исправлено: `cd "$(dirname "$0")"` в начале release (работает независимо от того, откуда скрипт запущен).
- [x] проверить start, release — оба скрипта прошли `bash -n` (синтаксис ОК); package.json содержит скрипт "tauri", так что `npm run tauri dev` / `npm run tauri build` из start рабочие; sed-замены версий проверены на тестовых копиях файлов.

## Улучшения по запросу (после сверки)
- Подсказки навигации в меню: раньше (и в легаси, и по инерции в
  HintBar.svelte) hint-bar на экране меню всегда скрывался (цифры 1-9
  на пунктах меню считались достаточной подсказкой). По просьбе — теперь
  меню тоже показывает hint-bar: "↑↓ выбрать · 1-9 быстрый выбор ·
  Enter начать". Заодно убраны 2 дублирующихся локальных объявления
  HINT_MENU (в MenuScreen.svelte и auth.js) в пользу одного
  экспортируемого из quiz.js — раньше при рассинхроне текста подсказки
  в этих трёх местах пришлось бы править в трёх местах.
- start: не было shebang — `./start` падал с "Exec format error"
  (годится было только `sh start`). Добавлен #!/bin/bash + флаг:
  `./start` — dev, `./start build` — production-сборка Tauri.
- .github/workflows/release.yml — принесён из старого (легаси) репозитория
  пользователем и сверен с текущим состоянием проекта: версии Node/Rust,
  SDK-расширения flatpak-builder (node22/rust-stable, runtime 50) и пути
  сборки (frontendDist "../build" в tauri.conf.json ↔ adapter-static
  pages/assets "build" в svelte.config.js) — всё совпадает 1-в-1 с тем,
  что уже настроено в репозитории после миграции на SvelteKit, изменений
  не потребовалось. Два job'а: "release" (tauri-action, Windows nsis +
  macOS universal dmg/app, публикует GitHub Release draft) и
  "release-flatpak" (собирает и подписывает Flatpak, публикует OSTree-репо
  на GitHub Pages, прикрепляет .flatpak к тому же релизу). Секреты
  (FLATPAK_GPG_PRIVATE_KEY, FLATPAK_GPG_PASSPHRASE) не мои — добавляются
  пользователем в настройках репозитория.
  
  
## Сделано по последнему списку todo
- `.svelte-kit/` добавлен в .gitignore (не был перечислён — каталог
  генерируется SvelteKit'ом при dev/build и не должен трекаться).
- Titlebar.svelte: аватар в шапке всегда рендерил только букву-инициал,
  даже когда у пользователя загружено `profile_photo` (в ProfileScreen.svelte
  фото уже показывалось). Теперь Titlebar использует ту же логику
  avatarStyle(), что и ProfileScreen — фото, если есть, иначе цветной кружок
  с инициалом.
- friends.svelte.js/FriendsPanel.svelte: панель друзей грузила данные только
  один раз за всё время жизни приложения (`if (!friends.loaded) loadFriends()`
  в onMount, а флаг loaded, once true, никогда не сбрасывался) — новые заявки
  или принятые с другой стороны заявки друзей не появлялись в профиле до
  перезапуска приложения. Теперь loadFriends() вызывается при каждом
  монтировании панели (открытии профиля).
- Убрана Tab-навигация/фокус на кастомных списках с собственной клавиатурной
  навигацией (стрелки/цифры/Enter через document-level keydown в
  MenuScreen/ChaptersScreen/QuestionScreen/RandomCountScreen/ResultScreen) —
  `tabindex="0"` → `tabindex="-1"` на этих role="button"/role="checkbox"
  элементах, чтобы обычный Tab по ним не прыгал и не путал с
  клавишами-стрелками, которые и так двигают "active"-подсветку.
- Скроллбары: точечные `::-webkit-scrollbar` правила (были на
  .chapter-list/.split-detail/.q-options/.admin-table-wrap, 7-8px) убраны в
  пользу одного глобального тонкого скроллбара в reset.css (6px,
  `scrollbar-width: thin` + webkit-варианты), применённого ко всем
  скроллящимся областям, включая те, что раньше получали неоформленный
  системный скроллбар (.friends-list, .modal-body, списки в модалках
  редактора/поиска).
- Корона (👑, .crown-btn) в титлбаре — кнопка лидербоарда друзей — заменена
  на кубок (🏆), класс переименован в .trophy-btn (Titlebar.svelte +
  components.css).
- .gitignore: `node_modules/` — стандартное правило, рекурсивно исключает
  все файлы внутри каталога; в самом package-lock.json встречаются строки
  вида "node_modules/пакет" — это просто пути пакетов в дереве зависимостей
  (нормально для lock-файла), не трекаемые файлы. В присланном архиве нет
  .git, так что реальный git-статус проверить не удалось — если у
  пользователя node_modules всё же попадает в `git status` не как
  untracked/ignored, а как модификации трекаемых файлов, вероятная причина —
  файлы были закоммичены ДО появления этого правила в .gitignore, и тогда
  их нужно один раз убрать из индекса: `git rm -r --cached node_modules`
  (сам .gitignore трогать не нужно, он и так корректен).
- "Различные меню и т.д. не используют всю высоту, обрезаются" — не удалось
  локализовать по одному чтению исходников: .screen/.stage уже используют
  min/max-height:100% + overflow-y:auto специально против обрезания (см.
  комментарии в layout.css, это уже чинилось раньше, см. пункты 1-14 выше),
  и видимых элементов с overflow:hidden поверх реального контента (кроме
  однострочных text-overflow:ellipsis) не нашлось. Нужен скриншот или
  название конкретного экрана/окна, где видно обрезание, иначе есть риск
  сломать уже настроенную раскладку наугад.
- Структура проекта перенесена из README.md сюда (ниже) и исправлена под
  текущий SvelteKit-проект — старое описание в README ссылалось на легаси
  vanilla-JS раскладку (src/js/*.js, src/css/, index.html/app.js), которой
  в репозитории больше нет.

### Структура проекта (актуально, SvelteKit)
```
RoadWitsClient/
├── src/
│   ├── app.html
│   ├── routes/
│   │   ├── +layout.js            # SSR отключён (adapter-static)
│   │   ├── +layout.svelte        # общий каркас: Titlebar/StatusBar/Splash
│   │   └── +page.svelte          # единственный маршрут — экран переключается стейтом
│   └── lib/
│       ├── api/
│       │   ├── api.js            # общение с backend'ом
│       │   ├── cache.js          # офлайн-кэш вопросов/глав
│       │   └── questions.js      # загрузка/обновление вопросов и глав
│       ├── components/           # все экраны и модалки (*.svelte)
│       ├── styles/                # reset / variables / layout / components / fonts
│       ├── auth.js                # логин / автологин / логаут
│       ├── quiz.js                # логика прохождения теста, HINT_*
│       ├── admin.js               # редактирование контента/лицензий
│       ├── friends.svelte.js      # друзья и лидерборд
│       ├── device.js              # слой поверх Tauri invoke(), тач-детект
│       ├── update.js              # проверка обновлений по GitHub Releases
│       ├── config.js              # адрес backend'а
│       ├── state.svelte.js        # общее состояние приложения (рунный $state)
│       └── stores/ui.svelte.js    # тосты, модалки, hint-bar, подтверждения
├── static/                # фонты (self-hosted) и статические ассеты
├── src-tauri/              # Rust-ядро (Tauri)
│   ├── src/{main.rs,lib.rs}
│   ├── icons/
│   ├── capabilities/        # ACL / permissions Tauri
│   ├── tauri.conf.json      # frontendDist: "../build" (собирается vite build)
│   └── Cargo.toml
├── flatpak_data/            # всё для Linux/Flatpak-сборки
├── .github/workflows/release.yml   # CI: Windows/macOS/Linux + Flatpak
├── svelte.config.js         # adapter-static, pages/assets "build"
├── vite.config.js
├── start                    # шорткат: ./start (dev) / ./start build
├── release                  # бамп версии + тег + пуш релиза
└── package.json
```

## Найдено пользователем (баги в профиле/лидерборде/теме)
15. Тема (🌙/☀️ переключатель в профиле) не менялась визуально — тема
    сохранялась на сервере (`api.updateSettings`, `state.user.settings.theme`)
    и CSS для светлой темы уже существовал (`:root[data-theme="light"]` в
    components.css), но НИГДЕ в коде атрибут `data-theme` не проставлялся на
    `<html>` — переключатель менял стейт, но ничего физически не красил.
    Добавлен `$effect` в +layout.svelte (единственное место, трогающее этот
    атрибут), синхронизирующий `data-theme` с `isLightTheme()`.
16. Клик по фото профиля "выделял" его (не hover) — `.avatar-upload-btn:
    focus-visible .avatar-upload-hint{opacity:1}` показывает затемнение с
    текстом "Сменить фото" при фокусе клавиатурой, но WebKitGTK (Tauri на
    Linux) местами трактует :focus-visible и как обычный клик мышью — после
    клика оверлей "залипал". Исправлено добавлением `onmousedown={e =>
    e.preventDefault()}` на кнопку — клик мышью больше не фокусирует кнопку
    вообще (клик по-прежнему открывает выбор файла), а Tab+Enter с
    клавиатуры фокусирует и показывает подсказку как раньше.
17. В лидерборде, похоже, не было скролла — `.leaderboard-list` имел
    собственные `max-height`+`overflow-y:auto` ВНУТРИ `.modal-body`, у
    которого уже есть свой `overflow-y:auto` (специально чинилось раньше,
    см. пункт про modal-body выше) — два вложенных auto-скролл контейнера с
    разными эффективными высотами. Убран внутренний max-height/overflow у
    `.leaderboard-list`, единственный скролл-контейнер теперь `.modal-body`,
    как у всех остальных модалок (question-search и т.д. пока не трогал —
    баг не про них).
18. Кнопка "Сохранить" в личных данных профиля убрана — по описанию, она не
    нужна, раз уже есть автосохранение при уходе с экрана
    (savePersonalDataIfChanged, вызывается из back()). Теперь та же функция
    дополнительно вызывается по onblur каждого из трёх полей (имя/фамилия/
    email) — сохранение происходит сразу по уходу фокуса с поля, а не
    только при выходе из профиля целиком. Успешный тост убран (был бы
    навязчивым на каждый blur), вместо кнопки — короткая надпись
    "· сохраняем…" рядом с заголовком блока, видна только пока идёт запрос.
19. "Дефекты стиля" переключателя темы: (a) обе иконки 🌙/☀️ были одинаково
    приглушены (opacity:0.7) независимо от текущей темы — непонятно, какая
    активна, кроме как по положению бегунка; добавлена подсветка активной
    иконки через `.theme-switch:has(input:checked)` (по аналогии с уже
    использованным :has() в layout.css для hint-bar). (b) `.theme-switch`
    (label) не имел `position: relative`, хотя вложенный `input` спрятан
    через `position: absolute` — добавлен как корректный контекст
    позиционирования (сам чекбокс нулевого размера, поэтому визуально это
    не ломало, но было технически некорректно/хрупко).
    
todo:
-переделать стиль приложения, сделать красивее, анимации и тд. (частично: back-кнопки,
 hover на пунктах меню, accent-пресеты подключены к активным/выделенным состояниям —
 см. п.20 ниже; крупная визуальная переработка (шрифты/раскладка/иллюстрации) не начата)

## Сделано по последнему списку todo (2026)
20. Четыре пункта todo закрыты одним заходом:
    a) Фото вопроса/просмотрщик на весь экран "выделялось" при клике —
       та же причина, что и в п.16 (WebKitGTK трактует клик как фокус/selection).
       Добавлены user-select:none/-webkit-user-drag:none/tap-highlight:transparent
       на картинку и onmousedown-preventDefault на кликабельную обёртку — и на
       превью в вопросе (.q-image-wrap), и на сам ImageViewer.
    b) Настройки вынесены из профиля в отдельный экран (SettingsScreen.svelte,
       screen: "settings"): тема, акцентный цвет, офлайн-кэш, вход в админку
       (для admin) и "О программе" — переехали туда. В профиле остались личные
       данные/фото/лицензия/друзья/выход и одна кнопка "⚙️ Настройки" наверху.
       Admin/Credits теперь возвращаются на "settings", а не на "profile"
       (кнопка "← Настройки"); Titlebar.openProfile() учитывает "settings" в
       группе профильных экранов (та же защита от Esc-пинг-понга, что раньше
       была только для profile/admin/credits). Смена темы/акцента сохраняется
       сразу по клику (как и раньше было с темой) — то есть даже надёжнее, чем
       "по выходу" из профиля: ничего не остаётся неотправленным при закрытии.
    c) Акцентный цвет: 6 пресетов (янтарный/синий/зелёный/фиолетовый/розовый/
       бирюзовый) через `--accent`/`--accent-ink` в variables.css
       (:root[data-accent="..."]), выбор — кружки-свотчи в SettingsScreen,
       синхронизация атрибута — эффект в +layout.svelte (по аналогии с
       data-theme). Точечно переведены на var(--accent) те места, что по
       смыслу — "выделение/интерактив", а не светофорная семантика:
       активный пункт меню, отмеченная/активная глава, выбранный вариант
       ответа, фокус полей, hover иконок/кнопок. Статусные цвета (соединение,
       кэш degraded/ok, таймер обратного отсчёта, кнопки титлбара) оставлены
       буквальным amber/green/red — они про светофор ПДД, а не про акцент.
    d) Кнопки "← Назад"/"← Меню"/"← Профиль" и т.п. (.profile-back/.screen-back)
       были голым `.ghost.small` — теперь пилюля на --panel-2, с hover в
       акцентный цвет и лёгким сдвигом влево (transform: translateX(-2px)),
       и небольшим "нажатием" (scale) по клику. Заодно у пунктов меню
       (.menu-item) появился hover для мыши (раньше подсветка была только у
       .active, наведение никак не отзывалось) — фон + сдвиг вправо на 2px.
## Правки по фидбеку после п.20 (тема/отступы/кнопки/лидерборд)
21. Тема оформления в настройках — раньше подпись "Тема оформления" стояла
    строкой над переключателем (как остальные блоки настроек — они про
    списки/действия, а не про одиночный контрол), выглядело неровно рядом
    с остальными. Обёрнуто в `.settings-row` (flex, space-between) — подпись
    слева, переключатель справа, в одну строку.
22. Отступы в настройках — SettingsScreen.svelte был единственным экраном
    с "карточным" контентом (в отличие от ProfileScreen/CreditsScreen),
    у которого не было обёртки `.profile-card` (панель с бордером и
    padding:24px) — блоки настроек сидели прямо в `.screen`, у которого
    padding даёт только внешний `.stage` (это отступ окна, а не панели),
    из-за чего внутренние ряды (свотчи цвета, статус кэша) визуально
    залезали к самому краю панели. Обёрнуто в `.profile-card`, как у
    Profile/Credits — теперь единообразно с остальными "карточными" экранами.
23. Единый стиль кнопок навигации:
    a) `.q-phase-arrow` (стрелки "Фаза X из Y" в топбаре) был скруглённым
       квадратом (`--radius-sm`), а `.q-arrow-btn` (стрелки между вопросами
       в тренировке по главам) — кругом (50%) — те же по смыслу "prev/next"
       кнопки выглядели по-разному. Компактный вариант оставлен меньше по
       размеру (контекст — вписан в высоту топбара), но форма приведена
       к кругу, как у второго.
    b) `.q-btn-finish` ("Завершить") и `.q-btn-continue` ("Продолжить") не
       имели hover/active transform, в отличие от уже анимированных
       `.q-arrow-btn`/`.profile-back`/`.menu-item` — добавлен тот же язык
       (лёгкий подъём на hover, сжатие на active).
    c) `.q-btn-continue` использовал буквальный `var(--amber)` для текста и
       фиксированный `rgba(255, 176, 32, ...)` для hover-фона вместо
       `var(--accent)` — при смене акцентного пресета (см. п.20c) кнопка
       оставалась жёстко янтарной, хотя рамка уже красилась в акцент:
       разные части одной кнопки не совпадали по цвету. Приведено к
       `--accent` целиком (текст и hover через `color-mix`).
24. Кубок-кнопка лидерборда в титлбаре была иконкой без подписи (только
    title-тултип при наведении) — по просьбе добавлена надпись "Лидерборд"
    рядом с 🏆, кнопка стала пилюлей вместо круглой иконки. На узких
    окнах/тач (`max-width: 700px`) подпись скрывается обратно до одной
    иконки — по тем же соображениям места, что и раньше у tooltip-only
    варианта.

Более широкая визуальная переработка (типографика, иллюстрации, переходы
между экранами) по-прежнему не начата — из исходного todo это самая
расплывчатая часть, проще всего двигать отдельными задачами со ссылкой на
конкретный экран/скриншот, а не одним общим "сделать красивее".

## Правки по todo-списку (README/мобильные кнопки/настройки/апдейт/лидерборд/экзамен)
25. a) README — описание технологий/модулей отставало от реальной архитектуры:
       всё ещё говорило про "чистый JS без фреймворков и бандлера" и
       перечисляло модули (`render.js`, `state.js`, `friends.js`) из
       дореформенной структуры (см. п.15-16 выше — переход на SvelteKit/
       Svelte 5 runes). Обновлено: SvelteKit/Svelte 5 + Vite, актуальный
       список модулей (`api/*.js`, `*.svelte.js`, `components/*.svelte`),
       команда сборки фронтенда упомянута явно (`tauri build` сам зовёт
       `vite build`), раздел про релиз дополнен — `./release` теперь
       правит версию и в `package.json` тоже (не только tauri.conf/Cargo.toml).
    b) Мелкие кнопки на тач-устройствах — `.icon-btn` (26px/22px) и
       `button.small` (padding 5px/9px, 12px текст) были рассчитаны на
       мышь и заметно меньше рекомендуемого минимума ~40-44px для пальца.
       Добавлен `@media (pointer: coarse)` блок, увеличивающий именно эти
       классы (плюс кубок/настройки в титлбаре) до тач-дружелюбных
       размеров — раскладка и десктопный (курсорный) вид не меняются.
    c) Настройки перенесены из профиля в титлбар, рядом с лидербордом —
       кнопка "⚙️ Настройки" добавлена в Titlebar.svelte (та же логика
       группы экранов/profileReturnScreen, что у "🏆 Лидерборд"/openProfile),
       старая ссылка "⚙️ Настройки" внутри ProfileScreen убрана.
       SettingsScreen.back() раньше жёстко вёл на "profile" (единственный
       вход был оттуда) — теперь, как и у Profile, возвращает на
       profileReturnScreen (экран, с которого открыли), иначе "← Назад" из
       настроек, открытых прямо из меню/тренировки, уводил бы в профиль,
       которого пользователь и не открывал.
    d) Кнопки выхода "← Назад"/"← Меню" у Profile/Settings/Credits раньше
       были отдельным элементом НАД карточкой (`.profile-card`/
       `.credits-card`), а не внутри неё — перенесены внутрь, первым
       дочерним элементом карточки. Заодно RandomCountScreen (тренировка по
       случайному билету → "сколько вопросов") — единственный из
       "менюшных" экранов без рамки вокруг списка (в отличие от `.split` у
       ChaptersScreen и `.profile-card` у Profile/Settings/Credits) —
       обёрнут в такую же `.profile-card` с кнопкой "← Меню" внутри неё.
    e) Обновления на Windows не скачивались/не устанавливались — реальная
       причина: WebView2/Tauri оборачивает свой процесс в Job Object с
       KILL_ON_JOB_CLOSE, и `std::process::Command::new(&path).spawn()` без
       доп. флагов создаёт инсталлятор ВНУТРИ того же Job — когда фронтенд
       сразу вслед за этим закрывал приложение (`exit(0)`, штатное
       поведение — установщику нужно, чтобы файлы Roadwits никто не
       держал), Windows в большинстве случаев убивал вместе с родителем и
       только что запущенный, ещё не успевший инициализироваться NSIS-
       инсталлятор: скачивание проходило успешно, а установка — нет, без
       видимой ошибки. Добавлены флаги `CREATE_NEW_PROCESS_GROUP |
       DETACHED_PROCESS` (`std::os::windows::process::CommandExt::
       creation_flags`) — отвязывают инсталлятор от Job/процесса/консоли
       родителя, чтобы он пережил закрытие Roadwits.
    f) Flatpak-версия — раньше кнопка "Проверить обновления" под Flatpak
       только показывала тост "обновляйте вручную командой flatpak update".
       Добавлена команда `flatpak_update_and_restart` (lib.rs): через
       `flatpak-spawn --host` (портал org.freedesktop.Flatpak, новый
       `--talk-name` в finish-args манифеста) вызывает `flatpak update -y
       --noninteractive com.roadwits.client` на ХОСТЕ (сама песочница на
       чтение-только и обновить себя не может), а по успеху — перезапускает
       уже обновлённое приложение тоже через хост (`flatpak run`) и
       закрывает текущий процесс. Если portal недоступен/обновление не
       удалось — понятная ошибка тостом со старой инструкцией "вручную"
       как запасной вариант (не автопроверка — только по кнопке, чтобы не
       дёргать `flatpak update` в фоне без ведома пользователя).
    g) Полоса прокрутки при наведении на пункты меню — `.menu-item:hover`
       двигает элемент через `transform: translateX(2px)`, чисто визуально,
       но в некоторых webview это всё равно учитывается при расчёте
       overflow контейнера — у `.screen` был выставлен только
       `overflow-y: auto` (без явного overflow-x), из-за чего у самого низа
       экрана (над hint-bar, т.к. `.stage` кончается прямо над ним)
       на пару кадров проскакивала горизонтальная полоса прокрутки.
       Добавлен `overflow-x: hidden` — сдвиг на hover больше не считается
       переполнением по горизонтали.
    h) Лидерборд — прокрутка уже была почищена раньше (п.17: единственный
       скролл-контейнер `.modal-body`, без вложенного собственного скролла
       у списка) и работает; добавлен только отступ, которого не хватало:
       `.modal-body` намеренно не даёт верхний паддинг (нужно формам без
       зазора перед первым полем), из-за чего первая строка лидерборда
       прилипала прямо к нижней границе шапки модалки. `margin-top`
       добавлен точечно на `.leaderboard-list`, а не на `.modal-body`
       целиком — чтобы не сдвинуть остальные модалки, у которых так и было
       задумано.
    i) Медленная загрузка контрольного экзамена без кэша — экзамену нужно
       всего 10 вопросов, но для их выбора звался тот же `getQuestionPool()`,
       что и для "тренировки по случайному билету" (где действительно может
       понадобиться весь пул) — то есть без диска ЭКЗАМЕН СКАЧИВАЛ ВСЮ БАЗУ
       ВОПРОСОВ ПО ВСЕМ ГЛАВАМ ради десяти случайных. Добавлена отдельная
       `getExamQuestions()`: берёт до 10 случайных ГЛАВ и тянет по одному
       случайному вопросу из каждой (тот же принцип "билет из разных тем",
       что и на настоящем экзамене ПДД) — сеть/кэш трогает не больше 10 глав
       вместо всех. Если каких-то из выбранных глав не хватило (пустая/не
       загрузилась) — недостающее добирается из общего пула как раньше,
       чтобы билет не оказался короче 10 вопросов.
    j) Флаг Республики Беларусь в шапке меню стоял ПЕРЕД текстом
       ("🇧🇾 Учебная программа · Республика Беларусь") — переставлен после
       текста, как просили.

todo:
-более широкая визуальная переработка (типографика, иллюстрации, переходы между экранами) — по-прежнему не начата, см. комментарий выше

## Правки по фидбеку после п.25 (event.listen, флаг РБ, тема, кнопка "Меню")
26. a) `Unhandled Promise Rejection: event.listen not allowed` — enforceLandscapeWindow()
       в device.js зовёт `appWindow.onResized(...)`, а это подписка через
       tauri-событие (`listen` под капотом) — у capabilities/default.json не
       было ни одного `core:event:*` permission (только window/webview/app/
       process/opener), поэтому подписка падала с этой ошибкой при каждом
       запуске. Добавлен `core:event:default` в permissions.
    b) Флаг 🇧🇾 в шапке меню стал маленьким после переноса ПОСЛЕ текста (п.25j) —
       увеличенный размер (26px) был на селекторе `.belarus-mark span:first-child`,
       а после перестановки спанов первым ребёнком стал текст, а не флаг:
       текст невольно получил 26px, а флаг остался на размере контейнера (12px).
       Заменено на явный класс `.belarus-flag` вместо позиционного
       `:first-child`/`:last-child` — не зависит от порядка спанов в разметке.
    c) Кнопка "← Меню" в тренировке по главам (ChaptersScreen) была отдельным
       элементом НАД рамкой `.split`, как раньше и "← Назад" в Settings —
       перенесена внутрь: первым элементом в `.panel-header` левой колонки,
       рядом с "Выберите главу" (обёрнуты в `.panel-header-left`, чтобы
       `justify-content: space-between` по-прежнему разводил их группу и
       `.panel-actions` по разным краям).
    d) Тема оформления — иконки 🌙/☀️ были описаны как строка (`.theme-switch`:
       inline-flex, единственная ось), но по репорту рендерились друг над
       другом (луна над переключателем, солнце под ним). Не нашёл в CSS
       правила, которое явно ломало бы направление — но раз баг воспроизвёлся
       в реальной сборке, поставил защиту явно, а не полагался на умолчания
       flex: `.theme-switch` теперь с explicit `flex-direction: row;
       flex-wrap: nowrap`, у `.theme-switch-icon`/`.theme-switch-track`
       добавлен `flex: 0 0 auto`, чтобы ни один из трёх элементов (иконка/
       трек/иконка) не мог сжаться/перенестись на новую строку. Стоит
       перепроверить на реальной сборке — если стек всё ещё будет, дело не в
       направлении flex, а где-то глубже (например, шрифт/дефолтный
       user-agent stylesheet для `<label>` внутри WebView2).

## Правки по фидбеку после п.26 (найдена настоящая причина луны/солнца, ещё разрешение, регресс экзамена)
27. a) Луна/солнце в теме оформления — настоящая причина найдена: у
       `.profile-settings label` (специфичность 0,1,1 — класс + элемент)
       было `flex-direction: column`, и это ПЕРЕБИВАЛО `.theme-switch`
       (специфичность 0,1,0 — просто класс) несмотря на мой `flex-direction:
       row` из п.26d — специфичность важнее порядка/направления в самом
       правиле. `.profile-settings label` рассчитан на другой паттерн
       (`<label>Имя<input/></label>` в ProfileScreen — подпись НАД полем,
       колонка) и никак не должен был трогать `.theme-switch`. Исправлено
       точечно: `.profile-settings label:not(.theme-switch)`.
    b) `.settings-row` — по просьбе убран `justify-content: space-between`
       (подпись и переключатель теперь просто в ряд с gap, без разъезда по
       краям строки).
    c) `window.set_size not allowed` — та же причина, что и у event.listen
       в п.26a: `appWindow.setSize(...)` в enforceLandscapeWindow (device.js)
       требует `core:window:allow-set-size`, которого не было в
       capabilities/default.json (там были только close/minimize/maximize/
       unmaximize/start-dragging). Добавлено.
    d) РЕГРЕСС из п.25i: "только 4 вопроса вместо 10" в контрольном
       экзамене — моя же правка (getExamQuestions, см. п.25i) брала
       `need = min(EXAM_QUESTION_COUNT, chapters.length)` — то есть если
       глав в базе меньше 10 (например, 4), билет обрывался на 4 вопросах
       независимо от того, сколько вопросов реально было в этих главах.
       Перепутал "количество глав" с "количество вопросов". Исправлено:
       теперь `need` считается по реальному числу вопросов —
       `chapters[].count` (question_count с бэкенда, уже есть в metadata
       списка глав бесплатно, без похода в сеть за вопросами) — набираем
       случайные главы по очереди, суммируя их count, пока не наберём
       нужное число "на бумаге", и только эти главы реально запрашиваем
       (Promise.all, как и раньше). Если у каких-то глав count не
       задан/устарел — сумма не дотянет, и в выборку уйдут все главы
       (тот же результат, что был до п.25i, просто как честный запасной
       случай, а не как правило по умолчанию).

## Правки по фидбоку после п.27 (тема/акцент без интернета)
28. Тема и акцентный цвет применялись только ПОСЛЕ успешного ответа сервера
    (`api.updateSettings`) — при ошибке сети переключатель ещё и откатывался
    обратно с тостом "Не удалось сохранить". Офлайн (без сети) сменить тему/
    акцент было в принципе невозможно. Теперь `saveSettings()` в
    SettingsScreen.svelte применяет новое значение СРАЗУ локально
    (`appState.user.settings`, откуда его читает $effect в +layout.svelte
    и красит `data-theme`/`data-accent` на `<html>`) и сразу же кэширует
    через `cache.setCachedUser` (тот же дисковый кэш профиля, что и для
    офлайн-входа, см. auth.tryAutoLogin) — так что тема/акцент переживают
    даже перезапуск приложения без сети. Запрос на сервер (`api.
    updateSettings`) уходит следом, но уже в фоне и best-effort: при ошибке
    сети локальное значение больше не откатывается — вместо тоста-ошибки
    теперь мягкий info-тост "применено локально, досохранится при связи".



TODO:
при создании или редактировании если пишет что есть точно такой же вопрос, то хотелось бы мини превью его увидеть по клику на слова точно такой же, подчеркнуть их чтобы было понятно, был баг что при редактировании билета если я нажму на изменение фото а потом не буду сохранять билет то старое фото затерется. нет ховеров у крестиков модалок, нужен такой же как у кнопки завершить. хотелось бы по второму нажатию на ответ (цифрой, мышью, enter, тапом) делать тоже самое что и кнопка продолжить. при выходе из аккаунта в модалке нужно писать product key с возможностью скопировать в стиле как при создании аккаунта. есть много мест где акцентный цвет не применяется (используется дефолтный). в модалке по случайному билету нельзя выбрать цифрами. в отвеченном билете если я нажимаю цифру то билет прорешивается повторно, не знаю как вдругих режимах, но в контрольном экзамене точно, нужно убрать возможность отвечать на билет каким либо способом если он решен. Хочу избавиться от ошибок svetle check
