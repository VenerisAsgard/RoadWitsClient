pub mod jwt;
pub mod product_key;

// Почему в этом крейте нет argon2 (и не было причин её держать):
// в исходной системе нет паролей — единственный секрет пользователя это
// product_key (см. app/models/user.py: "Пароля нет и не будет"), и логин
// требует точного O(1)-поиска по нему (`SELECT ... WHERE product_key = $1`,
// см. user_repository.get_by_product_key). Argon2 нарочно медленный и
// солёный — с ним такой индексированный поиск невозможен без перебора всей
// таблицы. product_key хранится как есть, той же уникальной индексированной
// колонкой, что и в оригинале. Если в системе когда-нибудь появится вход по
// паролю — зависимость нужно будет добавить заново тогда же.
