/**
 * ЕДИНСТВЕННОЕ место с адресом backend'а. Перенесено из src-legacy/js/config.js
 * без изменений — см. пояснения там же про CORS_ORIGINS на сервере.
 */
export const SERVER_BASE_URL = "http://leased-line-gomel-91-149-169-88.telecom.by:8000";

/** Таймаут одного запроса к серверу, мс. */
export const REQUEST_TIMEOUT_MS = 15000;

/** Таймаут для запросов с фото профиля — самое тяжёлое тело во всём
 * приложении (обычно 60-250 КБ после сжатия при текущем AVATAR_SIZE=640,
 * см. ProfileScreen.svelte; GIF — до ~4 МБ, см. MAX_PHOTO_DATA_URL_LENGTH
 * там же). На небыстром канале (см. SERVER_BASE_URL — арендованная линия)
 * даже 45 секунд не всегда хватало и запрос иногда обрывался НАШИМ
 * таймаутом раньше, чем сервер успевал ответить, показывая "Сервер не
 * отвечает" при полностью рабочем сервере — увеличено с 45 до 90 секунд. */
export const PHOTO_UPLOAD_TIMEOUT_MS = 90000;

/** Таймаут для списка вопросов главы (GET /chapters/{id}/questions).
 * Ответ приходит целиком, одним телом, со ВСЕМИ фото вопросов главы сразу
 * (без постраничной подгрузки) — у глав с большим числом фото-вопросов это
 * легко несколько мегабайт JSON. Дефолтных REQUEST_TIMEOUT_MS (15с),
 * рассчитанных на обычные лёгкие запросы, на таких главах на небыстром
 * канале не всегда хватает — запрос обрывался по таймауту раньше, чем
 * сервер успевал ответить, и это ошибочно выглядело как "сервер не
 * отвечает" (см. api.listQuestions, questions.js refreshAllCache). */
export const QUESTIONS_FETCH_TIMEOUT_MS = 45000;

/** Как часто опрашивать GET /health для индикатора связи. */
export const HEALTHCHECK_POLL_MS = 120000;
